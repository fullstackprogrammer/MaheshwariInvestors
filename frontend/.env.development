# Development only (`npm run dev`). Overrides frontend/.env for this mode so a
# production API URL in .env does not break local work.
# Use 127.0.0.1 so the browser does not hang on IPv6 (::1) when nothing is listening there.
VITE_API_BASE_URL=http://127.0.0.1:8080
